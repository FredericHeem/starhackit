{
  "name": "superliquid",
  "version": "1.0.0",
  "private": false,
  "scripts": {
    "install": "(cd client && npm install) && (cd server && npm install)",
    "start": "cd server && npm start",
    "test": "cd server && npm test"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:FredericHeem/superliquid.git"
  },
  "dependencies": {
    "codeclimate-test-reporter": "0.0.4",
    "rabbitmq-pubsub": "0.0.1"
  }
}
